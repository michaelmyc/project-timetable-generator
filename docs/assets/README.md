# 文档配图

本目录存放文档引用的图片资源（架构图、流程图、截图等）。

## 约定

- 优先用 Mermaid 代码块内嵌于文档，位图仅用于无法用代码表达的场景。
- 文件命名小写 kebab-case，如 `generator-flow.png`。
- 文档内引用用相对路径，如 `../assets/generator-flow.png`。
- 图源（如 Excalidraw / draw.io 工程文件）可同目录存放，命名 `<name>.source.<ext>`。
